var nowhs=[] ; 
const concatArrays = (string, ...array) =>{
  array.unshift(string);
  const fullarray=array.flat(); 
  console.log(fullarray);
    fullarray.forEach((str) => {
    //if (str.includes(string)) {
    //  fullarray.delete(str);
   // }
    fullarray.unshift;
      
  
    const nowhitespace = (fullarray) => {
         
         array = fullarray.map(line => line.trimEnd());
    console.log(array);
    
    };
    
    
    });  
      
     
      
 return array; 	
    
};

concatArrays('r', ['robi', 'géza'], [' márton'], ['hammadata']);